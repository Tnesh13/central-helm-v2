apiVersion: v1
kind: Service
metadata:
  name: {{ include "microserviceA.fullname" . }}
  labels:
    {{- include "microserviceA.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microserviceA.selectorLabels" . | nindent 4 }}
